import React from 'react';

const CategoryPage = ({name}: {name:string}) => {

  return (
    <div className='mt-14'>
      <h2>Category Page: {name}</h2>

      {/* Other content related to the category page */}
    </div>
  );
};

export default CategoryPage;
